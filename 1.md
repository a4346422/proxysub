# 免费节点自动采集测活

从公开订阅源采集节点，经 Xray 内核**实际测活**（走节点请求 `google/generate_204` 判 204），
每小时自动更新订阅。GitHub Actions 上运行，无需服务器。

> 把下面 `USER/REPO` 换成你自己的仓库路径。

## 订阅地址

| 客户端 | 地址 |
|---|---|
| v2rayN / v2rayNG / Nekobox | `https://raw.githubusercontent.com/a4346422/proxysub/main/output/sub.txt` |
| Clash / Mihomo / Clash Verge | `https://raw.githubusercontent.com/a4346422/proxysub/main/output/clash.yaml` |
| sing-box (SFI/SFA/SFM) | `https://raw.githubusercontent.com/a4346422/proxysub/main/output/singbox.json` |
| 明文链接（人工查看） | `https://raw.githubusercontent.com/a4346422/proxysub/main/output/nodes.txt` |

**国内直连 `raw.githubusercontent.com` 常不稳定**，可换 jsDelivr 镜像：

```
https://cdn.jsdelivr.net/gh/USER/REPO@main/output/sub.txt
```

jsDelivr 有约 12 小时 CDN 缓存，拿到的可能不是最新一轮 —— 要最新就用 raw 地址。

运行状态见 [`output/status.json`](output/status.json)（含真实更新时间、存活数、协议分布），
各源存活率见 [`output/source_stats.json`](output/source_stats.json)（每轮自动更新，也显示在 Actions 运行摘要里）。

## 部署

1. Fork 或新建仓库，把本目录内容推到默认分支
2. Settings → Actions → General → Workflow permissions 选 **Read and write permissions**
   （否则 workflow 无法把订阅提交回仓库）
3. Actions 页面手动跑一次 `Update Subscription` 验证，之后每小时自动执行

## 自定义

**改订阅源**：编辑 [`sources.txt`](sources.txt)，一行一个 URL，Clash YAML 类型的源在 URL 后加 ` clash`。改这个文件不用动代码。

> 采集日志里的 `+0` **不代表该源无用**。同仓库的链接式版本先被收录时，其 Clash 版本就会显示 `+0`，但它可能仍有独有端点。判断是否真冗余要看**端点差集**：
> ```
> python -c "
> import sys; sys.path.insert(0,'scripts')
> import collect
> from clash_parse import parse_clash
> main={collect.endpoint(n) for n in [collect.parse(l.strip()) for l in collect.try_b64decode(collect.fetch('<主力源URL>')).splitlines() if l.strip().startswith(collect.SCHEMES)] if n}
> mine={collect.endpoint(n) for n in parse_clash(collect.fetch('<待查源URL>'))}
> print('独有端点:', len(mine - main))"
> ```
> 差集为 0 才是真冗余。据此已移除 `mahdibland` 的两个 `Eternity` 榜单（端点 100% 被其 `sub_merge_base64.txt` 覆盖）。

**日期滚动命名的源**：有些仓库按日期命名文件（如 `v20260726`），硬编码第二天就 404。在 URL 里用占位符 `{YMD}`（也支持 `{Y}` `{m}` `{d}`），会自动展开成最近 `DATE_LOOKBACK_DAYS` 天（默认 3）；过期日期返回 404 自动跳过。

**改运行参数**：Actions 页面手动触发时可临时覆盖；要改默认值就编辑 `.github/workflows/update.yml` 的 `env:`。

| 变量 | 默认 | 作用 |
|---|---|---|
| `BATCH_SIZE` | 100 | 每批 socks 入站数 |
| `PROBE_TIMEOUT` | 8 | 测活超时（秒） |
| `PROBE_WORKERS` | 100 | 测活并发 |
| `TCP_WORKERS` | 400 | TCP 预筛并发 |
| `TCP_CHUNK` | 2000 | 预筛分片大小，防大规模并发假阴性 |
| `TCP_RECHECK` | 1 | 对判定不可达者的复查轮数（0 = 关闭） |
| `TEST_URL` | `https://www.google.com/generate_204` | 测活目标 |
| `INCREMENTAL` | 1 | 增量模式：先复测老兵再探索新节点（0 = 全量重跑） |
| `PROBE_BUDGET_SEC` | 1920 | B 段（探索）时间预算，到点停在当前批次（0 = 不限） |
| `VET_MISS_LIMIT` | 3 | 老兵连续多少轮测不通就淘汰 |
| `VET_MAX` | 400 | 老兵名单上限，超出保留命中多、miss 少的 |
| `MIN_ALIVE_TO_COMMIT` | 1 | 存活数低于此值时**不提交**，保住上一轮的可用订阅 |
| `MAX_NODES` | 32000 | 候选池上限，超出则按轮次偏移抽样（0 = 不限） |
| `DEDUP_ENDPOINT` | 1 | 端点级去重开关，设 0 则保留同端点的全部 UUID |
| `CHECK_IP` | 1 | 查出口 IP / 真实国家，设 0 关闭（省约 1–2 分钟） |
| `IP_WORKERS` | 3 | 出口查询并发（走节点带宽，高了会互相拖垮） |
| `LATENCY_MAX_MS` | 2000 | **少而稳**：写入订阅的延迟上限（ms），超过丢弃 |
| `MAX_OUTPUT_NODES` | 100 | **少而稳**：订阅最多保留条数 |
| `DEDUP_EXIT_IP` | 1 | **少而稳**：同一出口 IP 只留延迟最低的 1 条 |

**少而稳（对外订阅）**：测活仍可能得到数百节点，但写入 `output/` 前会再过滤——默认只保留延迟 ≤2000ms、按出口 IP 去重后最多 100 条。老兵名单 `veterans.json` **不裁剪**，以免下轮 A 段无兵可测。`status.json` 里 `alive_raw` 是测活原值，`alive` / `links` 是订阅实际条数。

**改频率**：`update.yml` 的 `cron: '17 * * * *'`。取非整点是因为 Actions 整点排队最重。
注意 GitHub 对 schedule **不保证准时**，延迟 10–30 分钟很常见，排得更密也不会真的更快。

## 工作流程

```
sources.txt (22 源，含日期滚动源自动展开)
    ↓ collect.py        解析 vmess/vless/trojan/ss + Clash YAML
    ↓                   两层去重：凭证级(协议|地址|端口|凭证) → 端点级(协议|地址|端口)
  ~3 万节点
    ↓ check_alive.py
    │   ┌─ A段 复测老兵：读 output/veterans.json，跳过 TCP 预筛直接测活
    │   │      ~60 个节点 / 约 17 秒 → 立刻落盘一份可用订阅垫底
    │   └─ B段 探索新节点（已剔除老兵）：
    │          ① TCP 端口预筛（分片 2000 + 复查）—— 滤掉约 45%
    │          ② 起 Xray 多入站，一节点一个 socks 端口，并发请求 generate_204
    │          受 PROBE_BUDGET_SEC 约束，到点停在当前批次
   两段合并
    ↓ check_ip.py       经节点自身查真实出口 IP / 国家
    ↓ formats.py        quality 少而稳（延迟≤2000 / 出口去重 / 上限100）→ 4 种订阅
    │                   veterans 不裁剪；status: alive_raw=测活, alive=订阅条数
output/
```

**测活是真·代理测活**，不是端口探测 —— 节点必须能实际把流量送出去并拿到 HTTP 204 才算通过。

**为什么分 A/B 两段（增量调度）**：原来每轮都把 3 万个节点从 TCP 预筛开始重跑，40 分钟后才知道存活集 —— 而这个集合和上一轮高度重叠，同一批机器不会一小时就全换掉。改成先复测「老兵」（历史测通过的节点）后，**17 秒就能得到 60 个存活**，剩下的时间全部用于探索新节点。

老兵名单存在 `output/veterans.json`，跟着仓库提交才能跨轮存活（runner 每轮都是全新容器）。淘汰规则是连续 `VET_MISS_LIMIT`（默认 3）轮测不通才移出 —— 免费节点本轮 timeout 下轮又活是常态，一次没通就删会把稳定节点误伤。本轮没排到队的老兵不扣分。

附带好处是抗超时：A 段结果立即写入 `alive.json`，即使 B 段被 runner 硬 kill，这轮也不会退化成空订阅。

**为什么要端点级去重**：公开池里大量节点是同一入口配几百个不同 UUID（实测单个端点被复用 602 次），测活结果必然相同。这层去重把 4.2 万压到 3 万，省掉约 30% 无谓测试。

**为什么 TCP 预筛要分片 + 复查**：早期版本一次性把 3 万个 socket 丢给线程池，产生大量假阴性。用 `scripts/exp_tcp_filter.py` 验证过：从"不可达"里随机抽 2000 个跳过预筛直接测活，**测出 5 个能通 204**，推算全池误杀约 55 个——比一整轮测出的 42 个还多。

根因不是超时太短：那 5 个节点单独测握手只要 0.2–0.4 秒，远快于 4 秒超时。是大规模并发下的资源竞争。同样 5 个已知可达节点，1000 样本规模稳定检出 5/5，3 万规模两次跑分别只检出 3/5 和 1/5，总可达数也在 7467~7608 间漂。

改成分片 2000 + 复查一轮后：检出 5/5，可达数从 7.5k 升到 16k（复查捞回 8186 个）。代价是单轮从 14.5 分钟涨到约 35 分钟，故 cron 改为每小时、`timeout-minutes` 提到 45。

## 实测数据（2026-07-26，本地）

| 阶段 | 数量 |
|---|---|
| 合并原始 | 43000+ |
| 两层去重后 | 30749 |
| Xray 可构造 | 30149（剔除老式 ss 加密、已移除的 h2 传输） |
| TCP 端口可达 | 7446（24.7%） |
| **实测通 204** | **19** |
| 全流程耗时 | 约 16.2 分钟（TCP 预筛 5.1min + 测活 11.1min） |

批量测活默认值来自全量实测对比：`BATCH_SIZE=100`、`PROBE_WORKERS=100` 是当前折中值。`90` 能测出更多节点但耗时约 27 分钟，太贴近 Actions 预算；`120/110/100` 耗时更短但存活数略少；`150/200` 速度更快但丢失更多存活节点。`TCP_CHUNK` 仍保持 2000。

### 节点名里的国家标注大量不实

流程会实测每个节点的真实出口 IP（`check_ip.py`，经节点请求 ip-api / ipinfo）。抽样结果：

| 节点名声称 | 真实出口 | ISP |
|---|---|---|
| 荷兰 | **US** 104.28.158.225 | Cloudflare |
| 俄罗斯联邦 | **DE** 170.101.109.52 | GTHost |
| 香港 | **TR** 152.233.53.89 | Datacamp |
| 🇩🇪 德国→🇹🇭 泰国 | TH 203.159.93.69 | Siamdata |
| CF中转 | **GB** 45.43.75.134 | YottaSrc |

所以**节点名只保留实测出口的国家码 + 国家名，并按同国家编号**，上游原名一并丢弃：

```
US 美国1        ← 104.16.107.139
US 美国2        ← 188.164.248.122   (原名"荷兰+V2CROSS.COM")
TH 泰国1        ← www.true.th       (原名"🇩🇪_DE_德国->🇹🇭_TH_泰国")
DE 德国1        ← 194.87.161.60     (原名"俄罗斯联邦_072619013")
UNKNOWN1       ← 出口查询失败，不猜测
```

输出会先按国家分组：香港（HK）、日本（JP）、新加坡（SG）、美国（US）优先，其余国家按国家码排序，未知国家最后。同国家内按上游节点名排序，再统一编号；编号本身即保证 Clash/sing-box 要求的名称唯一。

**还会检出共用出口的节点** —— 例如 `www.true.th`、`18.244.214.28`、`18.238.109.28` 三个不同入口，出口都是 `203.159.93.69`。它们是同一台落地机，在客户端里换着用没有意义。

### 各源存活率（单轮，31 个存活节点）

| 源 | 节点数 | 存活 | 存活率 |
|---|---:|---:|---:|
| `Pawdroid/Free-servers:sub` | 10 | 5 | **50.0%** |
| `ermaozi/get_subscribe:v2ray.txt` | 19 | 3 | **15.8%** |
| `free-nodes/v2rayfree:v{YMD}1` | 448 | 5 | 1.12% |
| `anaer/Sub:clash.yaml` | 281 | 3 | 1.07% |
| `Leon406/SubCrawler:vless` | 24632 | 15 | 0.06% |
| 其余 16 个源 | 5359 | 0 | 0 |

**节点数少的源存活率反而高**，这不是巧合：小而精的源（`Pawdroid` 只有 10 个节点）是人工筛选维护的；`SubCrawler` 那 2.5 万个是爬虫全量抓取，绝对贡献最大（15 个存活）但命中率只有 0.06%。

统计口径：节点归给**首个收录它的源**（端点级去重后），所以「节点数」是独有贡献，不是该源解析出的总数。这对后被处理的源略有不利，但避免一个节点被多源重复计数。单轮数据波动较大，判断源质量建议看多轮累积。

## 已知局限

- **可用节点很少**。5550 个采集节点只测出 28 个能用（0.5%）。公开池里约 70% 的服务器端口已死，剩下的多数也无法真正出墙。这是免费节点池的客观水位。
- **节点寿命极短**。回环复测时 28 个里有 3 个在几分钟内就掉线了。这正是每小时重跑的理由，但订阅里必然存在已失效项 —— 客户端里请自行测速剔除。
- **IP 都是共享机房 IP**。能访问 Google 没问题，但对风控敏感的场景（注册账号等）会被识别为数据中心流量。要干净 IP 只能用付费机场或自建。
- **不支持** hysteria2 / tuic / ssr / 老式 ss 加密（aes-256-cfb 等）—— 采集阶段直接丢弃。
- 订阅内容公开可见。这些本就是公开来源的节点，但**不要往 `sources.txt` 放私有订阅**。

## 安全提示

免费公开节点的运营者不可知，**流量可能被中间人查看**。不要用于登录重要账号、网银、传输敏感信息。仅适合看看公开资料这类低风险用途。

## 文件说明

| 文件 | 作用 |
|---|---|
| `scripts/collect.py` | 采集 + 解析 + 去重 |
| `scripts/clash_parse.py` | Clash YAML 解析（含不合规 YAML 的行级抢救） |
| `scripts/xray_batch.py` | 生成多入站 Xray 配置、启停内核 |
| `scripts/check_alive.py` | TCP 预筛 + 真·测活 |
| `scripts/to_link.py` | 节点 → 分享链接 |
| `scripts/quality.py` | 少而稳门禁：延迟上限 / 出口去重 / 条数上限 |
| `scripts/formats.py` | 4 种订阅格式输出（含 Clash/sing-box Reality 字段） |
| `scripts/summary.py` | Actions 运行摘要 |
